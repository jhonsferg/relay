version: 2

before:
  hooks:
    - go mod tidy

builds:
  - skip: true   # It's a library, not a binary

archives:
  - format: tar.gz

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
